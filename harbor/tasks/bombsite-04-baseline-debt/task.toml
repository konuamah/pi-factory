schema_version = "1.4"

artifacts = [
  { source = "/app/.factory/runs", destination = "factory-runs" },
]

[task]
name = "local/factory-bombsite-04-baseline-debt"
version = "1.0.0"
description = "Improve the contact form feedback while the repo contains unrelated broken-JS debt; the interview establishes scope boundaries and later stages must preserve them. Oracle validates; agent trials score Factory orchestration."
[[task.authors]]
name = "Kelvi"
keywords = ["factory", "harbor", "benchmark", "html", "javascript", "interview", "scope", "baseline-debt"]

[metadata]
family = "baseline-debt-triage"
stressSite = "bombsite"
pillarFocus = ["interviewQuality", "handoffQuality", "scopeQuality", "adaptationQuality"]
interviewRequired = true
networkPolicy = "public"

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 1200.0

[environment]
network_mode = "public"
build_timeout_sec = 900.0
cpus = 2
memory_mb = 2048
storage_mb = 10240
