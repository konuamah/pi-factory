Create `artifacts/factory-quality-summary.json`.

Required JSON object:

```json
{
  "repo": "pi-factory",
  "task": "factory-smoke",
  "status": "ok",
  "notes": "Harbor wiring is ready for richer Factory evals."
}
```

Do not modify files outside `artifacts/`.
